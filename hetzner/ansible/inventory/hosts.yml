all:
  children:
    coolify_hosts:
      hosts:
        box-a:
          ansible_host: 46.224.10.193
          box_role: primary
        box-b:
          # Fill in after `terraform output ipv4` in envs/box-b
          ansible_host: "PLACEHOLDER_BOX_B_IP"
          box_role: standby
